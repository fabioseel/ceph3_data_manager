[project]
name = "ceph3-data-manager"
version = "0.1.0"
description = "Simple S3 config YAML explorer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.37.0",
  "flask>=3.1.0",
  "omegaconf>=2.3.0",
  "pytest>=8.3.0",
  "pyyaml>=6.0.2",
]

[tool.uv]
package = false
